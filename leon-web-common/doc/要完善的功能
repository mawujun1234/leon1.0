1：修改Cnd类，让他支持M.StoreEquipment.num+"-1"这种方式
 * Cnd.update().set(M.StoreEquipment.num, M.StoreEquipment.num+"-1").andEquals(M.StoreEquipment.ecode, equipment.getEcode())
			.andEquals(M.StoreEquipment.store_id, outStore.getStore_id())
			
2：Cnd.toHql()的方法的参数。有问题，没有设计进去
				AbstractEntityPersister classMetadata=(AbstractEntityPersister)((SessionFactory)SpringContextHolder.getBean("sessionFactory")).getClassMetadata(StoreEquipment.class);
			String hql=Cnd.update().set(M.StoreEquipment.num, M.StoreEquipment.num+"-1").andEquals(M.StoreEquipment.ecode, equipment.getEcode())
			.andEquals(M.StoreEquipment.store_id, outStore.getStore_id()).toHql(classMetadata);
			
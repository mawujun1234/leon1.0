<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<!-- mawujun qq:16064988 e-mail:16064988@qq.com 承接各种项目 -->
<!-- 注意文件名称为:Example_${DbName}_Mapper.xml-->
<mapper namespace="com.mawujun.role.Role">
	<select id="queryByRole" resultType="com.mawujun.role.Role" parameterType="map">
    	SELECT a.*
	    FROM leon_Role a,leon_role_role b
		where a.id=b.current_id and b.other_id=#{otherId} and b.roleRoleEnum=#{roleRoleEnum}
		order by a.id
    </select>
	<!-- select语句的生成结束=============================================================================================== -->
	
	<insert id="insertRoleFun"  parameterType="com.mawujun.role.RoleFun">
		insert into leon_role_fun(id,role_id,fun_id,permissionEnum,createDate) VALUES(#{id},#{role.id},#{fun.id},#{permissionEnum},#{createDate});
	</insert>
</mapper>